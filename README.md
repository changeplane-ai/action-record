# action-record

A structured, validatable record of agent work. Every action an agent takes — the intent that started it, the context it consulted, the change it proposed, the policy decision it received, and the evidence backing it — is captured as a single strict-mode record that can be validated, reviewed, and audited. This repo defines the schema (Pydantic v2), a validator, and example records generated from real tool calls.
